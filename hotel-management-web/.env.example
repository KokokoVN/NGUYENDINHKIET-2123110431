# Để trống: dev dùng Vite proxy → http://localhost:5066 (cần chạy API trước).
# Nếu build static và mở file:// hoặc host khác, đặt URL API đầy đủ (cần CORS trên API):
# VITE_API_URL=http://localhost:5066
